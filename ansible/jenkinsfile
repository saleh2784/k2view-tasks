pipeline {
    options {
      ansiColor('xterm')
    }
    agent any
    
    parameters {
        choice (name: 'proccess', choices: ['install', 'uninstall', 'copy', 'ping'],)
    }

    stages {
        stage('Clean') {
            steps {
                cleanWs()
            }
        }
        stage('Get SCM') {
            steps {
                git branch: "main", url: 'https://github.com/saleh2784/k2view-tasks.git'
            }
        }
        stage('procees'){
        when {
          expression {
            params.proccess == "install"
          }
        }
            steps{
            ansiblePlaybook(
                // installation:'ansible', 
                // limit:'',
                playbook: "ansible/vim_install.yml",
                inventory: "ansible/host.ini",
                become: 'yes',
                extras: '-v',
                forks: 1,
                disableHostKeyChecking: true,
                credentialsId: 'ssh',
                colorized: true
          )
            }
        }
        // stage('procees ') {

		// 	steps {
		// 	    ansiblePlaybook(
        //         // installation:'ansible', 
        //         // limit:'',
        //         playbook: "ansible/vim_${params.proccess}.yml",
        //         inventory: "ansible/host.ini",
        //         become: 'yes',
        //         extras: '-v',
        //         forks: 1,
        //         disableHostKeyChecking: true,
        //         credentialsId: 'ssh',
        //         colorized: true
        //   )
		// 	}
		// }


    }

}
